apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: cpip

resources:
  - deployment.yaml

commonLabels:
  app.kubernetes.io/part-of: cpip
  app.kubernetes.io/managed-by: kustomize